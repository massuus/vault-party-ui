package dev.massuus.vaultpartyui.client.screen;

final class PartyPanelRenderState {
    final int scrollOffset;

    PartyPanelRenderState(int scrollOffset) {
        this.scrollOffset = scrollOffset;
    }
}
